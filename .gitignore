# Secrets / env
.env
.env.prod
*.local

# Downloaded artifacts
znuny/*.tar.gz

# OS / editor
.DS_Store
*.swp
*~

# Docker volume dumps / logs
*.log
/tmp/
.deploy-secrets.local

# Visual brainstorming companion (superpowers)
.superpowers/

# Python bytecode
__pycache__/
*.py[cod]

# PDFs de documentação são artefato de build, não fonte.
docs/pdf/out/

# Gravações brutas de cliente: pesadas e não são fonte — a transcrição versionada
# em docs/transcricoes/ é o que importa, e os stills relevantes estão em docs/assets/.
*.webm
*.mp4
*.mov
