# #1N — IA: sumarização + resposta sugerida (Ollama Cloud gpt-oss:120b) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`. Steps usam checkbox (`- [ ]`).

**Goal:** no console do agente, dois botões no detalhe do atendimento: **“Resumir com IA”** (resume a thread do ticket) e **“Sugerir resposta”** (gera um rascunho que o agente edita antes de enviar — nunca auto-envia). Motor = Ollama Cloud `gpt-oss:120b`.

**Architecture:** cliente async dedicado `integrations/ollama.py` (API nativa `/api/chat`, NDJSON, Bearer token, failure-safe). Domain `ai_service.py` puxa os artigos do ticket via GI de agente (`agent_get`), monta prompt, chama o LLM, registra em `ai_generation_log` (auditoria/custo). Endpoints **só de agente** (`gsid_adm`). Feature **opt-in** por env (`AI_FEATURES_ENABLED` + `OLLAMA_API_KEY`); some da UI quando off.

**Tech Stack:** httpx async, FastAPI, SQLAlchemy 2 async, Nuxt 3, pytest, vitest. Contexto do modelo: 128K tokens. **Egress externo** de conteúdo de ticket — ver invariante B do roadmap.

**Predecessor de migration:** `down_revision = "0015_csat"`.

---

### Task 1: Cliente Ollama — `integrations/ollama.py` + config

**Files:**
- Create: `apps/sidecar/src/gerti_sidecar/integrations/ollama.py`
- Modify: `apps/sidecar/src/gerti_sidecar/config.py` (5 env vars — ver roadmap §A)
- Test: `apps/sidecar/tests/test_ollama_client.py`

- [ ] **Step 1: Teste falhando** — com `httpx.MockTransport`, `chat()` monta `POST {base}/api/chat` com header `Authorization: Bearer <key>`, body `{model, messages, stream:false}`, e retorna `data["message"]["content"]`; 5xx/timeout → `OllamaUnavailable`; sem `api_key` → `OllamaDisabled`.

```python
# tests/test_ollama_client.py
import httpx, pytest
from gerti_sidecar.integrations.ollama import OllamaClient, OllamaUnavailable, OllamaDisabled

async def test_chat_happy(monkeypatch):
    def handler(req: httpx.Request) -> httpx.Response:
        assert req.url.path == "/api/chat"
        assert req.headers["authorization"] == "Bearer KEY"
        body = httpx.Request("POST", req.url, content=req.content).read()
        assert b'"model"' in body
        return httpx.Response(200, json={"message": {"content": "RESUMO"}, "done": True})
    client = OllamaClient(base_url="https://ollama.com", api_key="KEY", model="gpt-oss:120b",
                          transport=httpx.MockTransport(handler))
    out = await client.chat([{"role": "user", "content": "oi"}])
    assert out == "RESUMO"

async def test_chat_disabled():
    client = OllamaClient(base_url="https://ollama.com", api_key="", model="gpt-oss:120b")
    with pytest.raises(OllamaDisabled):
        await client.chat([{"role": "user", "content": "oi"}])
```

- [ ] **Step 2: Rodar** `pytest tests/test_ollama_client.py -v` → FAIL.

- [ ] **Step 3: Cliente** (failure-safe, espelha o padrão `_post` do Znuny; `transport` injetável p/ teste):

```python
# integrations/ollama.py
from __future__ import annotations
import json
from collections.abc import AsyncIterator
from typing import Any
import httpx

class OllamaError(RuntimeError): ...
class OllamaDisabled(OllamaError): ...        # sem api_key/feature off → 503/feature ausente
class OllamaUnavailable(OllamaError): ...     # transporte/timeout/5xx → 503

Message = dict[str, str]

class OllamaClient:
    def __init__(self, *, base_url: str, api_key: str, model: str,
                 timeout: float = 120.0, transport: httpx.AsyncBaseTransport | None = None) -> None:
        self._base = base_url.rstrip("/")
        self._key = api_key
        self._model = model
        self._timeout = timeout
        self._transport = transport

    def _headers(self) -> dict[str, str]:
        return {"Authorization": f"Bearer {self._key}", "Content-Type": "application/json"}

    async def chat(self, messages: list[Message], *, reasoning_effort: str = "low") -> str:
        if not self._key:
            raise OllamaDisabled("OLLAMA_API_KEY ausente")
        payload: dict[str, Any] = {
            "model": self._model, "messages": messages, "stream": False,
            "options": {}, "reasoning_effort": reasoning_effort,  # gpt-oss suporta low|medium|high
        }
        try:
            async with httpx.AsyncClient(timeout=self._timeout, transport=self._transport) as c:
                resp = await c.post(f"{self._base}/api/chat", headers=self._headers(), json=payload)
        except httpx.HTTPError as exc:
            raise OllamaUnavailable(str(exc)) from exc
        if resp.status_code >= 500:
            raise OllamaUnavailable(f"ollama http {resp.status_code}")
        if resp.status_code >= 400:
            raise OllamaUnavailable(f"ollama http {resp.status_code}: {resp.text[:200]}")
        data = resp.json()
        content = (data.get("message") or {}).get("content")
        if not content:
            raise OllamaUnavailable("resposta sem message.content")
        return str(content)

    async def chat_stream(self, messages: list[Message], *, reasoning_effort: str = "low") -> AsyncIterator[str]:
        """NDJSON: cada linha é um JSON; token incremental em message.content; fim em done=true."""
        if not self._key:
            raise OllamaDisabled("OLLAMA_API_KEY ausente")
        payload = {"model": self._model, "messages": messages, "stream": True, "reasoning_effort": reasoning_effort}
        async with httpx.AsyncClient(timeout=None, transport=self._transport) as c:
            async with c.stream("POST", f"{self._base}/api/chat", headers=self._headers(), json=payload) as resp:
                if resp.status_code >= 400:
                    raise OllamaUnavailable(f"ollama http {resp.status_code}")
                async for line in resp.aiter_lines():
                    if not line:
                        continue
                    chunk = json.loads(line)
                    piece = (chunk.get("message") or {}).get("content") or ""
                    if piece:
                        yield piece
                    if chunk.get("done"):
                        break
```

  E em `config.py`, adicionar os 5 campos (roadmap §A) + um factory `get_ollama_client(settings)`:
```python
def get_ollama_client(settings: Settings) -> OllamaClient:
    return OllamaClient(base_url=settings.ollama_base_url, api_key=settings.ollama_api_key,
                        model=settings.ollama_model, timeout=settings.ollama_timeout_seconds)
```

- [ ] **Step 4: Rodar** → PASS. **Commit:** `feat(#1N): cliente Ollama Cloud (api/chat NDJSON, failure-safe) + config`.

---

### Task 2: GI de agente — obter a thread completa do ticket

**Files:**
- Verify/extend: `apps/sidecar/src/gerti_sidecar/integrations/znuny_ticket.py` (`agent_get` deve retornar os **artigos** com corpo; se hoje só traz cabeçalho, estender)
- Verify/extend: `znuny/Custom/Kernel/GenericInterface/Operation/GertiTicket/AgentTicketGet.pm` (incluir `Articles` com `Subject`/`Body`/`From`/`CreateTime`/`IsVisibleForCustomer`)
- Test: `apps/sidecar/tests/test_agent_get_articles.py`

- [ ] **Step 1: Teste falhando** — mock GI retornando ticket com `Articles: [...]`; `agent_get` mapeia para um dataclass `AgentTicket` com lista de `Article(role, author, created, body)`.
- [ ] **Step 2: Rodar** → FAIL.
- [ ] **Step 3:** Se o `AgentTicketGet.pm` já devolve artigos, só mapear no Python; senão, estender o Perl (overlay `Custom/`, padrão dos outros ops) para incluir `Articles` (usar `ArticleList`+`ArticleGet` do Znuny, limitar corpo a, ex., 20 artigos mais recentes para caber no contexto). Mapear no `znuny_ticket.agent_get`.
- [ ] **Step 4: Rodar** → PASS. **Commit:** `feat(#1N): AgentTicketGet retorna artigos (thread) p/ IA`. (Se exigir mudança no Znuny, marcar p/ `Update --webservice-id 3` no deploy.)

---

### Task 3: Model `AiGenerationLog` + migration `0016`

**Files:**
- Create: `apps/sidecar/src/gerti_sidecar/models/ai_generation_log.py`
- Create: `apps/sidecar/alembic/versions/0016_ai_generation_log.py`
- Test: `apps/sidecar/tests/test_model_ai_log.py`

- [ ] **Step 1: Teste falhando** — insere log via `AdminSessionLocal` (sem RLS, cross-tenant como `agent_timer`) e lê de volta.
- [ ] **Step 2: Rodar** → FAIL.
- [ ] **Step 3: Model** (operacional, **sem RLS**; espelha `agent_timer`): colunas `id uuid`, `agent_login str`, `znuny_ticket_id int`, `kind str` (CHECK `summary|reply`), `model str`, `duration_ms int`, `ok bool`, `created_at`. Migration cria a tabela `gerti.ai_generation_log` sem policy RLS (owner admin, lida via BYPASSRLS), `down_revision = "0015_csat"`.
- [ ] **Step 4: Rodar** → PASS. **Commit:** `feat(#1N): ai_generation_log (auditoria/custo) + migration 0016`.

---

### Task 4: Domain `AiService` (sumarização + resposta sugerida)

**Files:**
- Create: `apps/sidecar/src/gerti_sidecar/domain/ai_service.py`
- Create: `apps/sidecar/src/gerti_sidecar/domain/prompts.py` (templates de prompt versionados)
- Test: `apps/sidecar/tests/test_ai_service.py`

- [ ] **Step 1: Teste falhando** — mock `ollama.chat` retornando texto fixo e mock `agent_get` com 3 artigos; `summarize(ticket_id, agent_login)` monta as `messages` (system + user com a thread) e retorna o texto; grava 1 linha em `ai_generation_log` com `kind="summary"`, `ok=True`. `suggest_reply(...)` idem com `kind="reply"`. Com `OllamaDisabled`, propaga (router vira 503/oculto).

- [ ] **Step 2: Rodar** → FAIL.

- [ ] **Step 3: Service** — `__init__(self, admin_session, ollama, gi)`:
  - `prompts.py`: `SUMMARY_SYSTEM` (“Você é um assistente de suporte técnico. Resuma o chamado em PT-BR: problema, o que já foi tentado, estado atual e próximo passo sugerido. Seja conciso e factual; não invente.”) e `REPLY_SYSTEM` (“Redija uma resposta profissional e empática em PT-BR ao cliente, com base no histórico. Não prometa prazos que não constam. Marque trechos a confirmar com [VERIFICAR].”). Funções `build_summary_messages(ticket)` e `build_reply_messages(ticket, instruction)`.
  - `summarize`: `t = await gi.agent_get(...)`; `msgs = build_summary_messages(t)`; cronometra; `out = await ollama.chat(msgs, reasoning_effort="low")`; grava log; retorna `out`. Envolver erros do GI/Ollama (log com `ok=False` em `finally`/`except` antes de re-raise).
  - `suggest_reply`: aceita `instruction: str | None` (ex.: “peça mais detalhes do erro”); idem.
  - Truncar a thread para caber no contexto (ex.: últimos N artigos / limite de chars) — função utilitária em `prompts.py`.

- [ ] **Step 4: Rodar** → PASS. **Commit:** `feat(#1N): AiService (summarize + suggest_reply) + prompts versionados`.

---

### Task 5: Endpoints de agente `POST /v1/admin/ai/summarize` e `/suggest-reply`

**Files:**
- Create: `apps/sidecar/src/gerti_sidecar/routers/admin_ai.py`
- Modify: `apps/sidecar/src/gerti_sidecar/main.py` (`include_router(admin_ai.router, prefix=...)`)
- Test: `apps/sidecar/tests/test_admin_ai_router.py`

- [ ] **Step 1: Teste falhando** — com `gsid_adm` válido: POST `{ticket_id}` → 200 `{text}`; sem sessão → 401; com `ai_features_enabled=False` → 404 (feature oculta); `OllamaUnavailable` → 503.
- [ ] **Step 2: Rodar** → FAIL.
- [ ] **Step 3: Router** — `dependencies=[Depends(get_admin_session)]`; checa `settings.ai_features_enabled` (senão 404); usa `AdminSessionLocal` (agente cross-tenant); instancia `AiService(get_ollama_client(settings), znuny_ticket)`; `summarize` e `suggest_reply` (body com `ticket_id` e, no reply, `instruction` opcional). Mapear `OllamaDisabled/OllamaUnavailable`→503, `ZnunyUnavailable`→503. (Streaming SSE fica como melhoria futura; MVP retorna texto completo.)
- [ ] **Step 4: Rodar** → PASS. **Commit:** `feat(#1N): endpoints admin/ai summarize + suggest-reply (opt-in, agente)`.

---

### Task 6: Console — botões IA no detalhe do atendimento

**Files:**
- Create: `apps/admin/server/api/admin/ai/summarize.post.ts`, `apps/admin/server/api/admin/ai/suggest-reply.post.ts`
- Create: `apps/admin/components/AiPanel.vue`
- Modify: `apps/admin/pages/atendimento/[id].vue` (painel IA + “usar como rascunho” na resposta)
- Modify: `apps/admin/composables/useAdmin.ts` ou novo `composables/useAi.ts` (helpers de fetch + flag `aiEnabled`)
- Test: `apps/admin/test/ai-panel.test.ts`

- [ ] **Step 1: Teste de componente** — `AiPanel` mostra botões “Resumir”/“Sugerir resposta”, estado loading, e emite `use-draft` com o texto; quando `aiEnabled=false`, o painel não renderiza.
- [ ] **Step 2: Rodar** vitest → FAIL.
- [ ] **Step 3:** Proxies (`sidecarFetch`, status passthrough); `AiPanel.vue` (Nuxt UI: `UButton` + `UCard` + `USkeleton` no loading + área de texto do resultado; botão “Usar como rascunho” no resultado de resposta). Em `atendimento/[id].vue`, renderizar `<AiPanel @use-draft="reply.body = $event">` para popular o textarea de resposta (agente edita e envia manualmente — **nunca** auto-send). Esconder se `aiEnabled` falso (descobrir via um GET leve `/api/admin/ai/enabled` ou flag no `/v1/admin/...`).
- [ ] **Step 4: Rodar** → PASS. **Commit:** `feat(#1N): painel IA no console (resumo + rascunho de resposta)`.

---

### Task 7: Deploy + e2e staging + docs

**Files:**
- Modify: `.env.prod` (no host de staging — **não commitar**): `OLLAMA_API_KEY=...`, `AI_FEATURES_ENABLED=true`.
- Modify: `docker-compose.yml` (passar as env vars `OLLAMA_*`/`AI_FEATURES_ENABLED` para `sidecar`).
- Modify: `.ia/INTEGRATION.md` (#1N), `.ia/OPS.md` (runbook: env vars, egress externo, `Update --webservice-id 3` se o AgentTicketGet mudou), `.ia/DECISIONS.md` (ADR: por que Ollama Cloud + `/api/chat` nativo + egress opt-in).

- [ ] **Step 1:** `make test` verde.
- [ ] **Step 2: Smoke do endpoint Ollama** (no host staging, dentro do container): `curl https://ollama.com/api/chat -H "Authorization: Bearer $OLLAMA_API_KEY" -d '{"model":"gpt-oss:120b","messages":[{"role":"user","content":"diga ok"}],"stream":false}'` → 200 com `message.content`. (Confirma key/limite/nome do modelo — ponto a verificar do roadmap.)
- [ ] **Step 3:** Deploy — migration 0016; `Update --webservice-id 3` se mexeu no AgentTicketGet; rebuild+up `sidecar` e `admin`.
- [ ] **Step 4: e2e** — como agente: abrir um ticket com histórico, “Resumir com IA” → resumo coerente em PT-BR; “Sugerir resposta” → rascunho popula o textarea; verificar 1–2 linhas em `gerti.ai_generation_log`. Desligar `AI_FEATURES_ENABLED` → painel some (provar kill-switch).
- [ ] **Step 5:** `.ia/` status “DEPLOYADO + e2e”. **Commit:** `docs(#1N): IA deployada em staging + e2e (Ollama gpt-oss:120b)`.

## Não-objetivos
Streaming SSE token-a-token (melhoria futura — cliente já tem `chat_stream`), fine-tuning, RAG sobre a KB (vem com o chatbot de deflexão, fora do top-5), auto-classificação/auto-resposta sem humano (a automação #1Q pode acionar IA, mas resposta ao cliente sempre passa por revisão), tradução.
