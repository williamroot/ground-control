# #1M — CSAT 1–5 no portal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` para implementar tarefa-a-tarefa. Steps usam checkbox (`- [ ]`).

**Goal:** quando um chamado é fechado, o cliente avalia o atendimento com nota **1–5** (+ comentário opcional) inline no detalhe do ticket no portal; a média alimenta o dashboard (#1O).

**Architecture:** tabela `csat_response` tenant-scoped (RLS, 1 resposta por ticket). Endpoint de cliente `POST /v1/tickets/{id}/csat` valida posse + estado fechado via GI `get_ticket` (já customer-scoped) e grava. O detalhe do ticket retorna o estado do CSAT (pendente/respondido) para a UI decidir mostrar o widget. Agregado para dashboard via `GET /v1/dashboard/metrics` (#1O lê desta tabela).

**Tech Stack:** FastAPI, SQLAlchemy 2 async, Alembic (RLS), Nuxt 3 (Nuxt UI), pytest+testcontainers, vitest.

**Predecessor de migration:** `down_revision = "0014_agent_timer"`.

---

### Task 1: Model `CsatResponse` + migration `0015_csat`

**Files:**
- Create: `apps/sidecar/src/gerti_sidecar/models/csat.py`
- Create: `apps/sidecar/alembic/versions/0015_csat.py`
- Test: `apps/sidecar/tests/test_model_csat.py`

- [ ] **Step 1: Teste falhando** — cria duas respostas em tenants distintos sob `tenant_session_scope` e prova isolamento RLS + UNIQUE por ticket.

```python
# tests/test_model_csat.py
import pytest
from sqlalchemy import select
from sqlalchemy.exc import IntegrityError
from gerti_sidecar.db import tenant_session_scope
from gerti_sidecar.models.csat import CsatResponse

async def test_csat_rls_and_unique(session, app_session_factory, seed_two_tenants):
    a, b = seed_two_tenants
    async with tenant_session_scope(a, factory=app_session_factory) as s:
        s.add(CsatResponse(tenant_id=a, znuny_ticket_id=10, customer_login="u@a", score=5))
    async with tenant_session_scope(b, factory=app_session_factory) as s:
        rows = (await s.execute(select(CsatResponse))).scalars().all()
        assert rows == []  # RLS: tenant b não vê o CSAT de a
    async with tenant_session_scope(a, factory=app_session_factory) as s:
        s.add(CsatResponse(tenant_id=a, znuny_ticket_id=10, customer_login="u@a", score=3))
        with pytest.raises(IntegrityError):
            await s.flush()  # UNIQUE (tenant_id, znuny_ticket_id)
```

- [ ] **Step 2: Rodar** `cd apps/sidecar && pytest tests/test_model_csat.py -v` → FAIL (no module / no table).

- [ ] **Step 3: Model** (espelha `models/agent_timer.py`; schema `gerti` implícito via `Base`):

```python
# models/csat.py
from __future__ import annotations
import datetime as dt
import uuid
from sqlalchemy import CheckConstraint, DateTime, ForeignKey, Integer, String, UniqueConstraint, text
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column
from gerti_sidecar.models.base import Base

class CsatResponse(Base):
    __tablename__ = "csat_response"
    __table_args__ = (
        UniqueConstraint("tenant_id", "znuny_ticket_id", name="ux_csat_ticket"),
        CheckConstraint("score BETWEEN 1 AND 5", name="ck_csat_score"),
        {"schema": "gerti"},
    )
    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))
    tenant_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("gerti.tenant.id"), nullable=False)
    znuny_ticket_id: Mapped[int] = mapped_column(Integer, nullable=False)
    customer_login: Mapped[str] = mapped_column(String, nullable=False)
    score: Mapped[int] = mapped_column(Integer, nullable=False)
    comment: Mapped[str | None] = mapped_column(String)
    created_at: Mapped[dt.datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=text("now()"))
```

- [ ] **Step 4: Migration** `0015_csat.py` (`down_revision = "0014_agent_timer"`), criando a tabela `gerti.csat_response` (mesmas colunas/constraints), depois **FORCE RLS + policy** copiando o padrão de `0007_cycle_consumption.py`:

```python
op.create_table("csat_response", ..., schema="gerti")
op.execute("ALTER TABLE gerti.csat_response ENABLE ROW LEVEL SECURITY")
op.execute("ALTER TABLE gerti.csat_response FORCE ROW LEVEL SECURITY")
op.execute(
    "CREATE POLICY csat_isolation ON gerti.csat_response USING ("
    "current_setting('app.current_tenant', true) = '' "
    "OR tenant_id = current_setting('app.current_tenant', true)::uuid)"
)
op.execute("GRANT SELECT, INSERT ON gerti.csat_response TO gerti_sidecar")
```
(Conferir o nome real do role app no INIT_SQL/migrations existentes — usar o mesmo GRANT das outras tabelas RLS.)

- [ ] **Step 5: Rodar** o teste → PASS. **Commit:** `feat(#1M): model csat_response + migration 0015 (RLS, UNIQUE por ticket)`.

---

### Task 2: Domain `CsatService` (validação de posse + estado fechado)

**Files:**
- Create: `apps/sidecar/src/gerti_sidecar/domain/csat_service.py`
- Modify: `apps/sidecar/src/gerti_sidecar/domain/errors.py` (adicionar `CsatError`, `TicketNotClosed`, `CsatAlreadyExists`)
- Test: `apps/sidecar/tests/test_csat_service.py`

- [ ] **Step 1: Teste falhando** — mock do `znuny_ticket.get_ticket` retornando `state="closed successful"` e dono = customer; `submit()` grava; segunda chamada → `CsatAlreadyExists`; ticket aberto → `TicketNotClosed`; ticket de outro cliente → `CsatError` (404-like).

```python
async def test_csat_submit_closed_ok(session, monkeypatch, seed_one_tenant_ctx):
    tenant_id, znuny_login = seed_one_tenant_ctx
    async def fake_get_ticket(*, ticket_id, customer_user, customer_id):
        return {"state": "closed successful", "customer_user": znuny_login}
    monkeypatch.setattr("gerti_sidecar.integrations.znuny_ticket.get_ticket", fake_get_ticket)
    svc = CsatService(session, znuny_ticket)
    r = await svc.submit(tenant_id=tenant_id, ticket_id=10, customer_login=znuny_login,
                         customer_id="AURORA", score=5, comment="ótimo")
    assert r.score == 5
    with pytest.raises(CsatAlreadyExists):
        await svc.submit(tenant_id=tenant_id, ticket_id=10, customer_login=znuny_login,
                         customer_id="AURORA", score=4, comment=None)
```

- [ ] **Step 2: Rodar** → FAIL.

- [ ] **Step 3: Service** — `__init__(self, session, gi)`; `submit(...)`:
  1. `t = await self.gi.get_ticket(ticket_id=..., customer_user=customer_login, customer_id=customer_id)` — `get_ticket` já é customer-scoped (404 se não for do cliente); se `None`/erro de posse → `raise CsatError("ticket_not_found")`.
  2. estado fechado: `if "closed" not in (t.get("state") or "").lower(): raise TicketNotClosed`.
  3. inserir `CsatResponse`; capturar `IntegrityError` (UNIQUE) → `raise CsatAlreadyExists`.
  4. retornar a linha.
  (Validar `score` 1–5 e truncar `comment` a, ex., 2000 chars antes de inserir.)

- [ ] **Step 4: Rodar** → PASS. **Commit:** `feat(#1M): CsatService com guarda de posse + estado fechado`.

---

### Task 3: Router `POST /v1/tickets/{id}/csat` + estado no detalhe

**Files:**
- Modify: `apps/sidecar/src/gerti_sidecar/routers/tickets.py` (novo endpoint + incluir `csat` no GET `/tickets/{id}`)
- Test: `apps/sidecar/tests/test_tickets_csat_router.py`

- [ ] **Step 1: Teste falhando** (TestClient + sessão `gsid` válida) — POST nota 5 em ticket fechado → 201; repetir → 409; ticket aberto → 422; sem sessão → 401. GET `/tickets/{id}` inclui `{"csat": {"submitted": true, "score": 5}}` ou `{"submitted": false, "eligible": true}`.

- [ ] **Step 2: Rodar** → FAIL.

- [ ] **Step 3: Endpoint** — `Body`: `CsatIn(score: int = Field(ge=1, le=5), comment: str | None = None)`; depende de `get_current_session`; usa `tenant_session_scope(session["tenant_id"])`; chama `CsatService.submit(...)` com `customer_login=session["znuny_login"]`. Mapear: `TicketNotClosed`→422, `CsatAlreadyExists`→409, `CsatError`→404, `ZnunyUnavailable`→503. No GET `/tickets/{id}`, após montar o ticket, consultar `csat_response` do ticket (mesma sessão tenant-scoped) e anexar bloco `csat` (submitted/score; `eligible = estado fechado and not submitted`).

- [ ] **Step 4: Rodar** → PASS. **Commit:** `feat(#1M): POST /v1/tickets/{id}/csat + estado csat no detalhe`.

---

### Task 4: Proxy Nuxt + widget no detalhe do ticket (portal)

**Files:**
- Create: `apps/portal/server/api/portal/tickets/[id]/csat.post.ts`
- Create: `apps/portal/components/ticket/CsatPrompt.vue`
- Modify: `apps/portal/pages/tickets/[id].vue` (renderizar o widget quando `ticket.csat.eligible`)
- Test: `apps/portal/test/csat-prompt.test.ts`

- [ ] **Step 1: Teste de componente (vitest)** — `CsatPrompt` emite `submit` com `{score, comment}`; usa cores **semânticas** (não a marca) para as notas; estado “respondido” mostra a nota e some o form.

- [ ] **Step 2: Rodar** `cd apps/portal && npm run test:run` → FAIL.

- [ ] **Step 3a: Proxy** (padrão `sidecarFetch`, guard de id `/^[0-9]+$/`, status passthrough):

```typescript
// server/api/portal/tickets/[id]/csat.post.ts
export default defineEventHandler(async (event) => {
  const id = getRouterParam(event, 'id')
  if (!/^[0-9]+$/.test(id ?? '')) { setResponseStatus(event, 400); return null }
  const body = await readBody(event)
  const { status, data } = await sidecarFetch(event, `/v1/tickets/${id}/csat`, { method: 'POST', body })
  setResponseStatus(event, status)
  return data
})
```

- [ ] **Step 3b: Componente** `CsatPrompt.vue` — 5 botões (estrelas/carinhas `i-lucide-star`/faces), `UTextarea` opcional, `UButton` enviar; `defineEmits<{ submit: [{score:number, comment:string}] }>()`. Cores: escala neutra→`warning`→`success` (semântica), **nunca** `--brand-primary` para a nota.

- [ ] **Step 3c: Página** — em `pages/tickets/[id].vue`, se `ticket.csat?.eligible` mostra `<CsatPrompt @submit="sendCsat">`; `sendCsat` faz `$fetch('/api/portal/tickets/{id}/csat', {method:'POST', body})`, em sucesso troca para o estado “Obrigado! Sua avaliação: N”; trata 409/422 com `useToast`.

- [ ] **Step 4: Rodar** vitest → PASS. **Commit:** `feat(#1M): widget CSAT no detalhe do ticket (portal) + proxy`.

---

### Task 5: Docs + deploy + e2e staging

**Files:**
- Modify: `.ia/INTEGRATION.md` (linha #1M na tabela de features), `.ia/OPS.md` (runbook deploy: migration 0015 + sem mudança no Znuny).

- [ ] **Step 1:** `make test` (sidecar + portal verdes; 24 asserts da stack intactos).
- [ ] **Step 2:** Deploy staging — `git pull`; `sidecar-migrate` (alembic upgrade head → 0015); rebuild+up `sidecar` e `portal`.
- [ ] **Step 3: e2e** — como cliente Aurora: fechar (ou usar ticket já fechado), abrir o detalhe, dar nota 5 + comentário → 201; recarregar → mostra “avaliado”; tentar de novo via API → 409. Verificar a linha em `gerti.csat_response`.
- [ ] **Step 4:** Atualizar `.ia/` com status “DEPLOYADO em staging + e2e”. **Commit:** `docs(#1M): CSAT deployado em staging + e2e`.

## Não-objetivos
NPS, envio por e-mail/token, lembrete/reenvio, CSAT por agente individual (vem do dashboard #1O lendo esta tabela), edição da nota pelo cliente.
